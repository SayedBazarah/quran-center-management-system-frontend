import type { ICourseRounds } from 'src/types/course';

import {
  Stack,
} from '@mui/material';


type Props = {
  rounds: ICourseRounds[];
};

export default function Rounds({ rounds }: Props) {
  return <Stack>ROUNDS</Stack>;
}
